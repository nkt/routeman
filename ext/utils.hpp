#ifndef ROUTEMAN_UTILS_HPP
#define ROUTEMAN_UTILS_HPP

#include <string>

namespace routeman {

    std::string str_replace(const std::string &str, const std::string &search, const std::string &replacement);

}

#endif
